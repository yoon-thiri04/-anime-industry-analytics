Flask>=3.1
gunicorn>=23.0
numpy>=1.26
pandas>=2.2
scipy>=1.13
# pinned exactly: the .pkl files in models/ were trained on scikit-learn 1.6.1 —
# a newer sklearn still loads them (with a harmless version-mismatch warning) but
# pinning avoids the warning and any future behavioural drift on the older estimators.
scikit-learn==1.6.1
xgboost>=2.1
shap>=0.46
joblib>=1.4
